# LilaKosha-MK1: Model Acquisition Blueprint
pipeline:
  - "acquire"

project:
    name: "LilaKosha"
    mark: "MK1"

volumes:
    raw: "${LILAKOSHA_VOLUME_RAW}" # Unified landing zone
    models: "${LILAKOSHA_VOLUME_MODELS}"
