# LilaKosha-MK1: Introspection & Recap-Augmentation Blueprint
# Pulled in one more record using ingest to bring total to 1000.
# Specifying only 500 records using start and stop UUID range.
# Running in parallel with 20260230080000-refine.yml
# Using Kilo Inference Gateway.
# Running only 'refine-characters'.
# Overall status before starting.
# INFO: ============================================================
# INFO: 📊 LILAKOSHA PIPELINE DATA HEALTH REPORT
# INFO: ============================================================
# INFO: Total Records Evaluated: 1000
# INFO: Healthy Records:         101 (10.10%)
# INFO: Defective Records:       899
# INFO: ============================================================
# Started around 2026-06-30 08:50:00 IST
pipeline:
    - "refine-characters"
    # - "refine-safety-dials"
    # - "refine-genre-theme"
    # - "refine-grammar"

project:
    name: "LilaKosha"
    mark: "MK1"
    generation: "G1"

volumes:
    raw: "${LILAKOSHA_VOLUME_RAW}"
    processed: "${LILAKOSHA_VOLUME_PROCESSED}"

services:
    local:
        provider: openai-compatible
        base_url: "${LILAKOSHA_SERVICE_LOCAL}"
        api_key: dummy
        model: local
    openrouter:
        provider: openai-compatible
        base_url: https://openrouter.ai/api/v1
        api_key: "${OPENROUTER_API_KEY}"
        model: "nvidia/nemotron-3-ultra-550b-a55b:free"
        # model: "openrouter/owl-alpha"
    kilo:
        provider: openai-compatible
        base_url: https://api.kilo.ai/api/gateway
        api_key: "${KILO_API_KEY}"
        model: "nvidia/nemotron-3-ultra-550b-a55b:free"

bindings:
    refine-characters:
        service: kilo
        temperature: 0.1
        max_tokens: 4096
        execution:
            requests_per_minute: 20

    # refine-genre-theme:
    #     service: kilo
    #     temperature: 0.1
    #     max_tokens: 2048
    #     execution:
    #         requests_per_minute: 20

    # refine-safety-dials:
    #     service: kilo
    #     temperature: 0.1
    #     max_tokens: 2048
    #     execution:
    #         requests_per_minute: 20

    # refine-grammar:
    #     service: local
    #     temperature: 0.0
    #     max_tokens: 2048
    #     execution:
    #         requests_per_minute: 20

parameters:
    start_uuid: 019f166a-c052-70c1-a807-29c5e9f1d299
    stop_uuid: 019f1687-6559-7851-97ea-6073303a38cf
