### TASK
Analyze the provided multi-turn chat history and classify the primary genre and extract thematic tags.

### CHAT EVIDENCE
{% set bot_name = session.meta.bot_name or "the character" %}

{% for item in session.items if item.kind == 'turn' -%}
    {% if loop.index0 < 100 -%}
        {%- set role = "USER" if item.actor_id == "user" else bot_name -%}
        {{ role }}: {{ item.prose }}
    {% endif %}
{%- endfor %}
